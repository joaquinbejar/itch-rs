Closes #45

## Summary

Implement the first slice of the itch-source crate (ADR-0012):
- MessageSource is a marker trait over Stream<Item = Result<Message, SourceError>> + Send + Unpin
- SourceError enum with Exhausted, Backend, Invariant variants
- SeqStore and SubscriptionPolicy trait stubs (full impls follow in #50-#51)
- Blanket impl for any Stream that matches the signature
- Three unit tests exercising blanket impl, error variants, and object-safety

All tests pass, clippy clean, forbid(unsafe_code) enforced.

## Checklist

- [x] MessageSource + SourceError compile and test
- [x] Blanket impl works
- [x] Clippy clean
- [x] forbid(unsafe_code) on lib.rs
- [x] No transport deps
