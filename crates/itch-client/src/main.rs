//! Demo ITCH 5.0 subscriber binary.
//!
//! See the workspace `README.md` for runtime instructions.
//! Override the connect address with `ITCH_SERVER` (default
//! `127.0.0.1:9100`).

#![forbid(unsafe_code)]

fn main() {
    // Stubbed in the workspace-scaffold issue; full implementation
    // lands in issue #11 (decode + pretty-print).
}
